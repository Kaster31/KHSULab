#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    setlocale(0, "RU");
/* 
    // ====== [ 6 Вариант ] ======
    // 1 задание
    double x = 16.55 * pow(10, -3), y = -2.75, z =0.15;
    double s = sqrt( 10*(pow(x, 1.0/3) + pow(x, y + 2)) ) * (pow(asin(z),2) - abs(x - y));
    cout << "[6.1] Ответ: " << s << endl;
    // 2  задание
    double p2, g2, s2, f2, t2, k2;
    
    cout << "[6.2] Введите f:";
    cin >> f2;
    cout << "[6.2] Введите k:";
    cin >> k2;
    
    t2 = f2 + pow(cos(k2),2);
    g2 = pow(t2, 3) + pow(abs(f2 * t2 - 5), 1.0/5);
    s2 = f2 + sin(pow(g2, t2));
    p2 = g2 + f2/s2 - exp(sqrt(abs(s2)));
    cout << "[6.2] Ответ: " << p2 << endl;

    // ===== [ 7 Вариант ] =====
    // 1 задание
    double x = 0.1722, y = 6.33, z = 3.25 * pow(10, -4);
    double s = (5 * atan(x)) - (1.0/4.0 * acos(x)) * (x + 3*(abs(x-y)) + pow(x, 2))/(abs(x-y)*z + pow(x,2));
    cout << "[7.1] Ответ: " << s << endl;
    // 2 задание
    double a2, b2, c2, d2, x2, y2;
    cout << "[7.2] Введите x:";
    cin >> x2;
    cout << "[7.2] Введите y:";
    cin >> y2;
    
    d2 = ( pow(exp(x2), 1.0/y2) - log(abs(x2*y2)))/(x2+y2);
    c2 = pow(d2, sqrt(x2*y2)) + exp(d2);
    b2 = pow(cos(c2), 2) + pow(sin(d2), 2);
    a2 = exp(b2) * exp(c2) * exp(d2);
    cout << "[7.2] Ответ: " << a2;
*/

    
    
    return 0;
}
